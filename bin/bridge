#!/bin/bash
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT" || exit 1
# shellcheck source=/dev/null
source "$ROOT/bin/grok-trust"

grok_trust_banner "Start Bridge" "$ROOT"
echo "Bridge listens for Grok chat/generate requests from Resolve."
exec python3 "$ROOT/grok_bridge.py" "$@"